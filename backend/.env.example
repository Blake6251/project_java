# docker-compose용 — 이 파일을 복사해 .env 로 저장한 뒤 값 수정
# cp .env.example .env

DOCKER_USERNAME=change-me-dockerhub-id
DB_PASSWORD=change-me-strong
JWT_SECRET=change-me-to-a-very-long-secret-at-least-32-bytes-for-hs256
